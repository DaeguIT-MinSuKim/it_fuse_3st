<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kr.or.dgit.donghun2.dao.CalculatedValueMapper">
	
	<select id="selectCalculatedValueByAll" resultType="CalculatedValue">
		SELECT scode, unitPrice, sellPrice, disprice, marginprice, marginPct
		from vw_calculate_sellInfo
	</select>
	
	<select id="selectCalculatedValueByNo" parameterType="CalculatedValue" resultType="CalculatedValue">
		SELECT scode, unitPrice, sellPrice, disprice, marginprice, marginPct
		from vw_calculate_sellInfo where scode= #{scode}
	</select>
	
</mapper>